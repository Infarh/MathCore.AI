name: Publish GitHub.com/packages

on:
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: ubuntu-latest
    name: Update package
    
    steps:
      
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 7.0.x
        
      - name: Building
        run: |
          dotnet build MathCore.AI -c Release --nologo
          dotnet build Tests/MathCore.AI.Tests -c Release --nologo
        
      - name: Testing
        run: dotnet test Tests/MathCore.AI.Tests -c Release --no-build --nologo
        
      - name: Packing
        run: dotnet pack MathCore.AI -c Release --no-build --nologo   
      
      - name: Push package
        run: dotnet nuget push "**/*.nupkg" --skip-duplicate -s https://nuget.pkg.github.com/Infarh/index.json -k ${{secrets.GITHUB_TOKEN}}
